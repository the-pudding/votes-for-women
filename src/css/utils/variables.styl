// -------------------
// Breakpoints
// -------------------
$mq_vars = {
    bp-2: '(min-width: 20em)' // 320px
    bp-3: '(min-width: 30em)' // 480px
    bp-4: '(min-width: 40em)' // 640px
    bp-5: '(min-width: 50em)' // 800px
		bp-6: '(min-width: 60em)' // 960px
		bp-m: '(min-width: 768px)'
		bp-d: '(min-width: 1048px)'
}

// -------------------
// Typefaces
// -------------------
$national = 'National 2 Narrow Web'
$tiempos = 'Tiempos Text Web'
$atlas = 'Atlas Grotesk Web'
$publico = 'Publico Text Web'

$sans-fb = Helvetica, Arial, sans-serif
$sans = $national, $sans-fb
$serif-fb = Georgia, Times, serif
$serif = $tiempos, $serif-fb
$serif-hed-fb = 'Times New Roman', serif
$mono = Menlo, Consolas, Monaco, 'Courier New', monospace

// z-index
$z-index = {
    bottom: -100
    middle: 0
    top: 100
    overlay: 1000
}

// Color
$black = #000
$off-black = #282828
$gray-dark = #5b5b5b
$gray = #949494
$gray-light = #c9c9c9
$off-white = #f4f4f4
$white = #fff

//Purples
$purple-10 = #201433
$purple-9 = #2b1b44
$purple-8 = #39245a //BASE
$purple-7 = #61507b
$purple-6 = #887c9c
$purple-5 = #b0a7bd
$purple-4 = #d7d3de
$purple-3 = #dfdce5
$purple-2 = #e7e5eb
$purple-1 = #efedf2 //USE AS WHITE

//ACCENTS
$magenta = #b50c7b
$teal = #339980
$gold = #fcc029

$bg = $white
$fg = $purple-10

$primary = $gold
$secondary = $teal


$fg-lighter = $purple-7
$fg-lightest = $purple-6

$bg-darker = $purple-1
$bg-darkest = $purple-3


$column-width = 40rem

$type-size = {
    xxx-small: {
        size: 10px,
        line-height: 1.4,
    }
    xx-small: {
        size: 11px,
        line-height: 1.4,
    }
    x-small: {
        size: 12px,
        line-height: 1.4
    }
    small: {
        size: 14px,
        line-height: 1.4    
    }
    root: {
        size: 16px,
        line-height: 1.4
    }
    medium: {
        size: 17px,
        line-height: 1.4
    }
    large: {
        size: 24px,
        line-height: 1
    }
    x-large: {
        size: 32px,
        line-height: 1
    }
    xx-large: {
        size: 40px,
        line-height: 1
    }
    xxx-large: {
        size: 48px,
        line-height: 1
    }
    xxxx-large: {
        size: 64px,
        line-height: 1
    }
}

$sidebar-width = 18rem
$sidebar-width-mobile = 18rem
$header-height = 3rem